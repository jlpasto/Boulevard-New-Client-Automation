services:
  - type: cron
    name: my-playwright-job
    env: docker
    schedule: "0 6 * * *"  # Runs at 1:00 AM CDT (6:00 AM UTC)
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PYTHON_VERSION
        value: 3.11